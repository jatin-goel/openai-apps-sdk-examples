[build]
builder = "nixpacks"
buildCommand = "corepack enable && cd widgets && pnpm install && pnpm run build && cd ../razorpay_server_node && pnpm install"

[deploy]
startCommand = "cd razorpay_server_node && pnpm run start"

